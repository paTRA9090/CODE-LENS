{
  "name": "streamify",
  "version": "1.0.0",
  "scripts": {
    "build": "npm instal --prefix backend && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm run start --prefix backend"
  },
  "devDependencies": {}
}